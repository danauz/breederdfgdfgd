import React, { useState } from 'react';
import { Layout, Calendar, Plus, Dog, Users, Activity, BarChart3 } from 'lucide-react';

const BreederApp = () => {
  const [activeTab, setActiveTab] = useState('dashboard');

  return (
    <div className="flex h-screen bg-slate-50 font-sans">
      {/* Sidebar */}
      <aside className="w-64 bg-slate-900 text-white flex flex-col p-6">
        <h1 className="text-xl font-bold mb-10 flex items-center gap-2 text-cyan-400">
          <Dog /> Demented Breeds
        </h1>
        
        <nav className="space-y-4 flex-1">
          <button onClick={() => setActiveTab('dashboard')} className={`flex items-center gap-3 w-full p-2 rounded ${activeTab === 'dashboard' ? 'bg-cyan-600' : 'hover:bg-slate-800'}`}>
            <Layout size={20} /> Dashboard
          </button>
          <button onClick={() => setActiveTab('dogs')} className={`flex items-center gap-3 w-full p-2 rounded ${activeTab === 'dogs' ? 'bg-cyan-600' : 'hover:bg-slate-800'}`}>
            <Activity size={20} /> My Dogs
          </button>
          <button onClick={() => setActiveTab('litters')} className={`flex items-center gap-3 w-full p-2 rounded ${activeTab === 'litters' ? 'bg-cyan-600' : 'hover:bg-slate-800'}`}>
            <Users size={20} /> Litters
          </button>
        </nav>

        <div className="text-xs text-slate-400 border-t border-slate-700 pt-4">
          Tasmania HQ | 2026
        </div>
      </aside>

      {/* Main Content Area */}
      <main className="flex-1 overflow-y-auto p-8">
        <header className="flex justify-between items-center mb-8">
          <h2 className="text-2xl font-bold capitalize text-slate-800">{activeTab}</h2>
          <button className="bg-cyan-600 text-white px-4 py-2 rounded-lg flex items-center gap-2 hover:bg-cyan-700 transition">
            <Plus size={20} /> New Entry
          </button>
        </header>

        {/* Dashboard View */}
        {activeTab === 'dashboard' && (
          <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
            {/* Quick Stats */}
            <div className="bg-white p-6 rounded-xl shadow-sm border border-slate-200">
              <h3 className="text-slate-500 text-sm font-medium">Active Dogs</h3>
              <p className="text-3xl font-bold text-slate-800 mt-2">12</p>
            </div>
            <div className="bg-white p-6 rounded-xl shadow-sm border border-slate-200">
              <h3 className="text-slate-500 text-sm font-medium">Expected Litters</h3>
              <p className="text-3xl font-bold text-cyan-600 mt-2">2</p>
            </div>
            <div className="bg-white p-6 rounded-xl shadow-sm border border-slate-200">
              <h3 className="text-slate-500 text-sm font-medium">Waitlist</h3>
              <p className="text-3xl font-bold text-slate-800 mt-2">45</p>
            </div>

            {/* Upcoming Tasks */}
            <div className="md:col-span-2 bg-white p-6 rounded-xl shadow-sm border border-slate-200">
              <h3 className="font-bold text-slate-800 mb-4 flex items-center gap-2">
                <Calendar size={18} className="text-cyan-600" /> Breeding Schedule
              </h3>
              <ul className="space-y-4">
                <li className="flex justify-between items-center p-3 bg-slate-50 rounded-lg">
                  <div>
                    <span className="font-semibold block">Luna (Rottweiler)</span>
                    <span className="text-sm text-slate-500 italic">Expected Whelping: May 12</span>
                  </div>
                  <span className="px-3 py-1 bg-amber-100 text-amber-700 rounded-full text-xs font-bold">14 Days Out</span>
                </li>
                <li className="flex justify-between items-center p-3 bg-slate-50 rounded-lg">
                  <div>
                    <span className="font-semibold block">Blue (Heeler)</span>
                    <span className="text-sm text-slate-500">Vaccination Due: May 20</span>
                  </div>
                  <span className="px-3 py-1 bg-blue-100 text-blue-700 rounded-full text-xs font-bold">Scheduled</span>
                </li>
              </ul>
            </div>
          </div>
        )}
      </main>
    </div>
  );
};

export default BreederApp;